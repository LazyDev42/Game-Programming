using UnityEngine;

public class ex2_lista2 : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        int dano = (15 + 5) * 2;
        print("O dano final é: " + dano);
    }

}