using UnityEngine;

public class ex6_lista2 : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created

    

    void Start()
    {
        float distancia = 120;
        float tempo = 10;
        float velocidade = distancia / tempo;
        print("A velocidade média do personagem é: " + velocidade);

    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
