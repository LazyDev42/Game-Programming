using UnityEngine;

public class Ex1_lista2 : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        int inimigosD = (3 * 10) + (2 * 25) + (1 * 100);
        print("valor total de inimgos derrotados é: " + inimigosD);
    }

}